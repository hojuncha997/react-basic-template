import React from 'react';
import ProductListComponent from '../../components/product/ProductListComponent';

const ProductListPage = ({ Children }) => {
    return (
        <div>
            <ProductListComponent/>
        </div>
    );
}
export default ProductListPage;
